# T4 — Änderungsgesuch (Runde 1: der Agenten-Prompt)

Lege eine neue Datei `src/clamp.js` mit einer exportierten Funktion
`clamp(value, min, max)` an, die `value` auf den Bereich `[min, max]` begrenzt.
Signatur exakt:

```js
export function clamp(value, min, max)
```

Lege außerdem `test/clamp.test.js` mit mindestens einem Test für die Funktion
an. Ändere keine anderen Dateien.

Abnahme: `npm test` läuft durch.
