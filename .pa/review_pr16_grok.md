I'll review the candidate against the live store code, focusing on the release guard, settlement, and whether "released unused" stays true.The forward path is fail-closed and atomic. Two gaps remain around rows that were already committed, and the new briefing field reports a release that has not happened.

**1. Medium.** `src-tauri/src/store/development_runs.rs:329`
`effectiveState` is derived only from `delivery.state == "started"` and `launch.state == "exited_undelivered"`. It does not look at the reservation or at `development_delivery_released`. For a row already committed under DF-15a, the launch is `exited_undelivered`, the delivery is still `started`, and the implementation reservation is still `started`, so `balance()` still counts those tokens (`development_budget.rs:116`). The briefing nevertheless sets `delivery.effectiveState` to `released_undelivered`. The same payload's `tokens` object still shows the hold. The cost receipt stays honest and takes the other branch (`development_usage_receipt.rs:216`), which says the reservation is retained. The two read models disagree until something actually cancels the row.

**2. Medium.** `src-tauri/src/store/native_completion.rs:374`, caller `src-tauri/src/process_capture/managed.rs:82`
The retroactive release runs only inside `commit_native_undelivered_exit`, and the only production caller is a live capture that still has the undelivered-exit object. Startup reconciliation (`src-tauri/src/main.rs:3581`) only moves `intent`/`launched` runs to `reconciling`. It never calls `release_undelivered_run_tokens`. After DF-15a, a successful finalize already returned, the host dropped the exit payload, and a second `launch_worker` on that run is refused. Nothing replays those rows, so the reservation stays `started` and the budget stays held. `KNOWN_ISSUES.md:53` says the replay branch frees DF-15a rows. That is true only when finalize runs again, which a finished host does not do. New exits in this commit are fine: the cancel and the `development_delivery_released` insert share the exit transaction, and a second call does not write another event.

**3. Low.** `docs/development/CONTINUOUS.md:517` and `:551`, `STAND.md:107`
The ledger contract still says a process exit never implies zero use and that every non-measured receipt keeps the whole reservation. This path now cancels a `started` implementation reservation and frees it in `balance()`. `STAND.md` still lists KI-27 as an open product question. Neither document changes runtime behavior. Both contradict the ledger this package writes.

`cancelled` does not mask a later settlement. `settle_bound` still requires `state = 'started'` and a launch in `exited` (`development_budget.rs:334` and `:358`). An `exited_undelivered` launch cannot enter that path, and nothing moves it back to `exited`. `actual_tokens` stays null, so the freed amount is unused ledger capacity, not a measured zero.

Verdict: **approve with conditions**. Derive `effectiveState` only when that run's implementation reservation is actually `cancelled`, and heal already-committed DF-15a rows with the same guard, or stop claiming replay does it.
