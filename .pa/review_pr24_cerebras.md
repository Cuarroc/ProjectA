**Review Findings**

| File | Line(s) | Severity | Issue |
|------|---------|----------|-------|
| `src-tauri/src/hooks.rs` | 89‑95 | Low | The new `coordinator_dir` function creates a sibling directory (`<hooks>-cwd`) but does not explicitly ensure the parent `<hooks>` directory exists before calling `ensure_private_dir`. If `settings_dir()` returned a path whose parent does not exist, `ensure_private_dir` could fail. Adding an `fs::create_dir_all` for the parent would make the function more robust. |
| `src-tauri/src/hooks.rs` | 278‑292 | Low | `ensure_outside_checkouts` uses `std::fs::canonicalize` to resolve the directory, which follows symlinks. A malicious symlink could point inside the repository while the canonicalized path appears outside (e.g., a symlink that resolves to a path under the repo after normalization). Consider also checking the *original* path components for `.git` ancestors to guard against such symlink tricks. |
| `src-tauri/src/profiles.rs` | 112‑123 | Low | The `for_coordinator` method overwrites `env_policy.isolation` but leaves `passthrough` unchanged. If a coordinator’s profile deliberately includes credential‑related environment variables in `passthrough`, they would still be exposed despite the strict isolation guarantee. A tighter implementation would clear or filter `passthrough` when switching to `Strict`. |
| `src-tauri/src/workers.rs` | 162‑172 | Low | The refusal message for a coordinator run uses the constant `ERR_REFUSED`. If this constant is not defined in the current module (it is defined elsewhere), the compilation will fail. Ensure the constant is imported or referenced correctly. |
| `src-tauri/src/workers.rs` | 382‑386 | Low | In the respawn logic the tuple `(profile, cwd)` is constructed regardless of whether `coordinator_dir` succeeded. In the error path (when `coordinator_cwd` fails) the function returns early, so this is safe, but the pattern could be clearer by separating the two branches to avoid accidental use of an uninitialized `cwd`. |
| `src-tauri/src/workers.rs` | 398‑400 | Low | The call to `agents.spawn_bound` now receives `&cwd` which is a `PathBuf`. The original signature expects a `&Path`. The implicit `Deref` works, but any future change to the signature could break this call. Explicitly pass `cwd.as_path()` to avoid reliance on deref coercion. |
| `src-tauri/src/workers.rs` | 447‑452 | Low | The test `a_coordinator_directory_inside_a_checkout_is_refused` creates a temporary directory named `"coordinator-dir-guard"` and then calls `ensure_outside_checkouts(&dir.path().join("missing"), &repo)`. If the temporary directory is removed before the call, the error message may be ambiguous (missing dir vs. inside checkout). This is just a test‑flakiness risk. |
| `src-tauri/src/workers.rs` | 560‑567 | Low | The test `a_coordinator_starts_outside_every_checkout_without_credentials` checks that `agents.isolations` contains only `EnvIsolation::Strict`. However, the test does not verify that the `passthrough` list is empty or unchanged, which could mask a credential leak. Adding an assertion on `passthrough` would improve coverage. |

**Overall Assessment**

The changes implement the required “coordinator runs without a write path” behavior correctly and include comprehensive tests. No critical bugs or safety regressions were identified. The low‑severity items above are minor robustness improvements and do not affect the functional correctness of the primary feature.
